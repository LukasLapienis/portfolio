import { ContactForm } from './ContactForm';
import { Contacts } from './Contacts';

export const Contact = () => {
  return (
    <section id="contact">
      <h3>Contact</h3>
      <ContactForm />
      <Contacts />
    </section>
  );
};
